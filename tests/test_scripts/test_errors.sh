#!/bin/bash
# Тестовый скрипт: обработка ошибок
# Содержит намеренные ошибки для проверки обработки

# Валидная команда
ls -la

# Неизвестная команда - должна вызвать ошибку
unknown_command

# Эта команда не выполнится без --debug
ls /home

# Еще одна неизвестная команда
invalid_cmd arg1 arg2